name: Kindle to X

on:
  schedule:
    - cron: '0 17 * * 0'   # Sunday 9am PST - generate
    - cron: '0 17 * * 1'   # Monday 9am PST - post tweet 1
    - cron: '0 20 * * 1'   # Monday 12pm PST - post tweet 2
    - cron: '0 23 * * 1'   # Monday 3pm PST - post tweet 3
    - cron: '0 17 * * 3'   # Wednesday 9am PST - post single
    - cron: '0 17 * * 5'   # Friday 9am PST - post single
  workflow_dispatch:
    inputs:
      mode:
        description: 'Mode to run: generate or post'
        required: true
        default: 'generate'

jobs:
  run:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pip install anthropic tweepy requests

      - name: Determine mode
        id: mode
        run: |
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            echo "mode=${{ github.event.inputs.mode }}" >> $GITHUB_OUTPUT
          elif [ "${{ github.event.schedule }}" = "0 17 * * 0" ]; then
            echo "mode=generate" >> $GITHUB_OUTPUT
          else
            echo "mode=post" >> $GITHUB_OUTPUT
          fi

      - name: Run script
        env:
          READWISE_TOKEN: ${{ secrets.READWISE_TOKEN }}
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          X_API_KEY: ${{ secrets.X_API_KEY }}
          X_API_SECRET: ${{ secrets.X_API_SECRET }}
          X_ACCESS_TOKEN: ${{ secrets.X_ACCESS_TOKEN }}
          X_ACCESS_SECRET: ${{ secrets.X_ACCESS_SECRET }}
          MODE: ${{ steps.mode.outputs.mode }}
        run: python main.py

      - name: Save state back to repo
        run: |
          git config user.name "github-actions"
          git config user.email "github-actions@github.com"
          git add state.json
          git diff --staged --quiet || git commit -m "Update state [skip ci]"
          git push
